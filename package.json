{
    "name": "peopleflow",
    "version": "1.0.0",
    "scripts": {
        "build": "npm install && cd client && npm install && npm run build",
        "start": "cd server && npm install && npm start"
    },
    "engines": {
        "node": "18.x"
    }
}